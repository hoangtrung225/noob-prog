#include <stdio.h>


int main(void)
{
int b;
printf("guess the average number of children per family in US :");
int check()
{
scanf("%d",&b);
if (b=2) { printf("that is correct\n"); break;}
else if (b<2)
{
printf("it is close abit higher \n make another one");
check();
}
else 
{
printf("it is close abit lower \n make another one");
check();
}
}
check();
return 0;
}
